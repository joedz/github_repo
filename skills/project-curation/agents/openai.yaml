display_name: Project Curation
short_description: Save impressive projects from GitHub or local repos into a structured, bilingual collection.
default_prompt: Curate or explicitly reclassify this project in the repository using the project-curation workflow, classify it by core workflow, choose an AI role when it belongs in ai-tools, keep only one canonical note path, and update the bilingual index using only categories that exist.
